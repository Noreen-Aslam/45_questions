// Store the author's name in a variable
let Author:string = "Albert Einstein";

// Output a quote by the author
console.log(`${Author} once said, "A person who never made a mistake never tried anything new."`);
