// Array of car models
let transpiration: string[] = ["Honda city", "Civic", "Rolls-Royce", "Bentley", "Ferrari", "Lamborghini"];

// Loop through each car model in the array
for (let N = 0; N < transpiration.length; N++) {
    // Output a statement expressing the desire to own each car model
    console.log(`I would like to own a ${transpiration[N]} car.`);
}
