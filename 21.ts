// Object containing information about a car
const car: { brand: string, model: string, year: number } = {
    brand: "Toyota",
    model: "Corolla",
    year: 2022
};

// Object containing information about a book
const book: { title: string, author: string, pages: number } = {
    title: "To Kill a Mockingbird",
    author: "Harper Lee",
    pages: 281
};

// Object containing information about a movie
const movie: { title: string, director: string, releaseYear: number } = {
    title: "The Shawshank Redemption",
    director: "Frank Darabont",
    releaseYear: 1994
};

// Print the objects
console.log("Car:", car); // Print the car object
console.log("Book:", book); // Print the book object
console.log("Movie:", movie); // Print the movie object
