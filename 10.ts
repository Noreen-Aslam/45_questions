// Array of names
let Names: string[] = ["Ayesha", "Fatima", "Aqsa", "Haleema", "Hareem", "Noreen"];

// Loop through each name in the array
for (let N = 0; N < Names.length; N++) {
    // Output a personalized greeting for each name
    console.log(`Hello, ${Names[N]}! Have a nice day.`);
}
